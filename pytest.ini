[pytest]
markers =
    unit: deterministic tests without external services
    integration: pipeline-level tests
    requires_ffmpeg: tests requiring local FFmpeg tools
    network: tests requiring external network services
filterwarnings =
    ignore:'audioop' is deprecated and slated for removal in Python 3.13:DeprecationWarning
    ignore:invalid escape sequence.*:SyntaxWarning
